# Copy of Record

Landing page for **Copy of Record** — a $19, one-time-purchase toolkit that turns a LinkedIn data export into a local, browser-only dashboard and scored contact list.

Static site: plain HTML/CSS, no build step, no server. Deployed on Vercel.
